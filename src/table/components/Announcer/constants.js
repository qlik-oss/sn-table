export const SN_TABLE_EVENT_TYPES = {
  EMIT_ANNOUNCEMENT: 'emit-announcement',
};

export const ASSERTIVE_PRESSURE = {
  ASSERTIVE: 'assertive',
  POLITE: 'polite',
  OFF: 'off',
};

export const uniqHash = '_' + Math.random().toString(36).substr(2, 12);
