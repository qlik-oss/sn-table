version: 2

jobs:
  empty:
    docker:
      - image: cimg/base:2023.06
    steps:
      - run:
          command: |
            echo "Empty workflow while switching"

workflows:
  version: 2
  build-all:
    jobs:
      - empty
